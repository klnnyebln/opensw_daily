#include <stdio.h>

int main() {
	int i_p, o_p;
	int sum = 0;
	int max = 0;

	for (int i = 0; i < 10; i++) {
		scanf("%d %d", &o_p, &i_p);
		sum += i_p - o_p;

		if (sum > max)
			max = sum;

	}
	
	printf("%d", max);

	return 0;
}
